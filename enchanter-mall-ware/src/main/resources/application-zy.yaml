server:
  port: 8084

spring:
  application:
    name: enchanter-mall-ware

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/gulimall_wms?useSSL=false&useUnicode=true&characterEncoding=UTF-8

mybatis:
  mapper-locations: classpath:/mapper/*Dao.xml

logging:
  level:
    com.erato.enchanter.mall.ware.dao: debug